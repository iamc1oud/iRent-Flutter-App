import 'package:flutter/material.dart';

class GuestInfoRegisterScreen extends StatelessWidget {
  final Map<String, dynamic> userData;

  const GuestInfoRegisterScreen({Key key, this.userData}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
